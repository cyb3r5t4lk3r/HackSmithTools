[package]
name = "network_diagnostis_tool" # Nebo jaký je název tvého balíčku
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.21.7"
rand = "0.8.5"
once_cell = "1.19"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true